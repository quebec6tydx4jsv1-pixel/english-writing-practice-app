<h1 class="text-2xl font-bold mb-6">反復練習添削結果</h1>

<div class="space-y-6">

  <!-- 模範解答 -->
  <div>
    <h2 class="text-xl font-semibold mb-2">模範解答</h2>
    <div class="p-4 border rounded bg-gray-50">
      <%= @review_answer.ai_correction.corrected_text %>
    </div>
  </div>

  <!-- あなたの回答 -->
  <div>
    <h2 class="text-xl font-semibold mb-2">あなたの解答</h2>
    <div class="p-4 border rounded bg-gray-50">
      <%= @review_answer.answer_text %>
    </div>
  </div>

  <!-- 克服した表現 -->
  <div>
    <h2 class="text-xl font-semibold mb-2">克服した表現</h2>
    <div class="p-4 border rounded bg-gray-50">
      <%= @uwe.mistake.expression_text %>
    </div>
  </div>

  <!-- 登録解除ボタン（右寄せ） -->
  <div class="mt-6 flex justify-end">
    <%= button_to "登録解除",
          user_weak_expression_path(@uwe),
          method: :delete,
          class: "px-4 py-2 bg-red-500 text-white rounded hover:bg-red-600 transition text-sm" %>
  </div>

  <!-- 弱点一覧に戻る（緑ボタン化） -->
  <div class="mt-6 flex justify-end">
    <%= link_to "弱点一覧に戻る",
        user_weak_expressions_path,
        class: "bg-green-600 text-white px-4 py-2 rounded hover:bg-green-700 transition text-sm inline-flex items-center" %>
  </div>

</div>
